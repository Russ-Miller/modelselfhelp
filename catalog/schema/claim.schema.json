{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://modelselfhelp.dev/schema/claim.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "capability",
    "statement",
    "kind",
    "backing_strength",
    "sources",
    "contested",
    "status",
    "last_checked_at",
    "submitted_by"
  ],
  "properties": {
    "id": {
      "$ref": "common.schema.json#/$defs/slug"
    },
    "capability": {
      "$ref": "common.schema.json#/$defs/slug"
    },
    "technique": {
      "$ref": "common.schema.json#/$defs/slug"
    },
    "conditions": {
      "type": "object",
      "additionalProperties": false,
      "description": "Applicability of the technique this claim is about, in a small controlled vocabulary an agent can filter on. Only on claims that set `technique`. Every value must be stated or clearly implied by the claim itself; absent fields mean the claim does not say.",
      "required": [
        "effect",
        "fails_when"
      ],
      "properties": {
        "effect": {
          "type": "string",
          "enum": [
            "helps",
            "narrows",
            "no-effect",
            "hurts"
          ],
          "description": "What this claim says the technique does to the capability: helps; narrows (helps only under the stated condition, or shifts rather than removes the failure); no-effect; hurts."
        },
        "needs": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": [
              "external-signal",
              "executable-environment",
              "retrieval-corpus",
              "fine-tuning-access",
              "separate-model",
              "evaluation-split",
              "complete-mediation",
              "raw-history"
            ]
          },
          "description": "What must be present for the technique to do its job. external-signal: a test result, compiler error, verifier output or environment outcome. executable-environment: code can be run. retrieval-corpus: documents or an index to retrieve from. fine-tuning-access: the weights can be trained. separate-model: a second model with different context. evaluation-split: a held-out set to gate changes. complete-mediation: the harness sees every side effect. raw-history: the unsummarised record stays retrievable."
        },
        "helps_most": {
          "type": "string",
          "enum": [
            "weaker-models",
            "stronger-models",
            "independent"
          ],
          "description": "Which models gain most, where the claim compares. independent: the effect does not track model strength."
        },
        "cost": {
          "type": "string",
          "enum": [
            "low",
            "moderate",
            "high"
          ],
          "description": "Tokens, latency or training relative to one unaided attempt, where the claim says."
        },
        "fails_when": {
          "type": "string",
          "minLength": 20,
          "description": "The condition under which the technique stops helping or does harm, as the claim states it."
        }
      }
    },
    "statement": {
      "type": "string",
      "minLength": 30
    },
    "tags": {
      "type": "array",
      "$ref": "common.schema.json#/$defs/slugList"
    },
    "kind": {
      "type": "string",
      "enum": [
        "mechanism",
        "observation"
      ]
    },
    "backing_strength": {
      "type": "string",
      "enum": [
        "single-paper",
        "replicated",
        "mechanism-reasoning",
        "own-observation"
      ]
    },
    "observed_on": {
      "$ref": "common.schema.json#/$defs/observedOn"
    },
    "sources": {
      "type": "array",
      "items": {
        "$ref": "common.schema.json#/$defs/sourceLink"
      },
      "minItems": 1
    },
    "contested": {
      "type": "boolean"
    },
    "disagreement_axis": {
      "$ref": "common.schema.json#/$defs/disagreementAxis"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending-review",
        "active",
        "superseded",
        "retired"
      ],
      "description": "pending-review: ingested but not yet endorsed by a human. Browsable and linkable, but deliberately inert \u2014 it does not move a technique's standing, does not count toward the backtest, and is excluded anywhere a claim would otherwise carry weight. active: reviewed and held. superseded / retired: no longer current."
    },
    "superseded_by": {
      "$ref": "common.schema.json#/$defs/slug"
    },
    "last_checked_at": {
      "$ref": "common.schema.json#/$defs/date"
    },
    "last_new_evidence_at": {
      "$ref": "common.schema.json#/$defs/date"
    },
    "notes": {
      "type": "string"
    },
    "submitted_by": {
      "$ref": "common.schema.json#/$defs/provenance"
    },
    "reviewed_by": {
      "type": "array",
      "description": "Who has read this claim against its sources: agent:... for a model, human:... for a person. Absent means derived from submitted_by: a human submitter has reviewed it; an agent submitter means AI review only.",
      "items": {
        "$ref": "common.schema.json#/$defs/provenance"
      },
      "uniqueItems": true
    }
  }
}
