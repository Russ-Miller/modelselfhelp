{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://modelselfhelp.dev/schema/source.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "kind",
    "title",
    "ingested_at"
  ],
  "properties": {
    "id": {
      "$ref": "common.schema.json#/$defs/slug"
    },
    "kind": {
      "type": "string",
      "enum": [
        "paper",
        "observation",
        "post",
        "vendor-doc"
      ],
      "description": "paper: a published or preprint work. observation: something the catalog's owner personally noticed. post: informal public writing by an individual. vendor-doc: guidance published by the party that builds the model. A vendor doc is privileged evidence about what its own model was trained to do -- nobody outside can measure that -- and structurally weak evidence that its advice works, because the evaluations behind it are not visible. It is also interested: a vendor recommends what shows its models well and lowers its support burden. Cap it at single-paper when it reports an actual measurement, mechanism-reasoning otherwise, and never replicated -- a vendor confirming its own guidance is not replication."
    },
    "title": {
      "type": "string",
      "minLength": 5
    },
    "authors": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "year": {
      "type": "integer",
      "minimum": 1990,
      "maximum": 2100
    },
    "date": {
      "$ref": "common.schema.json#/$defs/date"
    },
    "arxiv_id": {
      "type": "string",
      "pattern": "^\\d{4}\\.\\d{4,5}(v\\d+)?$"
    },
    "url": {
      "type": "string",
      "format": "uri"
    },
    "content_url": {
      "type": "string",
      "format": "uri",
      "description": "Direct link to the exact file read, where that differs from `url`. For a paper maintained in a repository this is the raw file, which is the canonical copy."
    },
    "content_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 of the file at content_url when it was read. Pins which version a claim rests on. Papers on arXiv get a version in their id; a PDF served from a repo can change under the same URL with nothing to notice it."
    },
    "content_checked_at": {
      "$ref": "common.schema.json#/$defs/date",
      "description": "When content_sha256 was last confirmed to still match."
    },
    "content_changed_at": {
      "$ref": "common.schema.json#/$defs/date",
      "description": "Set when the file at content_url stopped matching content_sha256. Claims citing this source need re-reading; the figures they quote may have been revised."
    },
    "author_handle": {
      "type": "string",
      "description": "For kind: post \u2014 the author's handle or username, e.g. \"@cherylwoooo\"."
    },
    "retrieved_at": {
      "$ref": "common.schema.json#/$defs/date",
      "description": "For kind: post \u2014 when the text below was captured. Required with archived_text."
    },
    "archived_text": {
      "type": "string",
      "minLength": 40,
      "description": "For kind: post \u2014 the post's text, verbatim, captured at retrieved_at. REQUIRED for kind: post. Posts are deleted and edited; a paper can be re-fetched years later and a post cannot. Without the archive the citation quietly stops being checkable, which is worse than not having it."
    },
    "venue": {
      "type": "string"
    },
    "summary": {
      "type": "string"
    },
    "notes": {
      "type": "string",
      "description": "Anything a reader should know about the source itself rather than its content -- provenance, authorship doubts, why it is filed as the kind it is. Distinct from summary, which describes what the source says."
    },
    "brief": {
      "type": "string",
      "minLength": 120,
      "description": "A reader-facing digest of the paper in the house summary style (docs/prompts/paper-summary.md): finding first, method, figures, boundary, takeaway. Generated from the abstract by scripts/summarize-sources.mjs and number-checked against it. Distinct from `summary`, which is a one-or-two-sentence factual gloss written by hand."
    },
    "brief_generated_at": {
      "$ref": "common.schema.json#/$defs/date"
    },
    "brief_model": {
      "type": "string"
    },
    "brief_source": {
      "type": "string",
      "enum": [
        "abstract",
        "full-text"
      ],
      "description": "What the brief was written from. Full text is available only for papers arXiv renders as HTML (roughly Dec 2023 onward); everything older falls back to the abstract, and its figures and evidence-quality paragraph are correspondingly thinner."
    },
    "brief_unverified_figures": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Numbers that appear in `brief` but not in the source abstract it was generated from. Should normally be empty; a non-empty list means the digest needs a human read before it is trusted."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "code_url": {
      "type": "string",
      "format": "uri"
    },
    "ingested_at": {
      "$ref": "common.schema.json#/$defs/date"
    },
    "citations_total": {
      "type": "integer",
      "minimum": 0
    },
    "citations_recent_12mo": {
      "type": "integer",
      "minimum": 0
    },
    "citations_checked_at": {
      "$ref": "common.schema.json#/$defs/date"
    },
    "semantic_scholar_id": {
      "type": "string"
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "kind": {
            "const": "post"
          }
        },
        "required": [
          "kind"
        ]
      },
      "then": {
        "properties": {
          "archived_text": {
            "type": "string"
          },
          "retrieved_at": {
            "type": "string"
          },
          "url": {
            "type": "string"
          }
        },
        "required": [
          "archived_text",
          "retrieved_at",
          "url"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "kind": {
            "const": "vendor-doc"
          }
        },
        "required": [
          "kind"
        ]
      },
      "then": {
        "properties": {
          "archived_text": {
            "type": "string"
          },
          "retrieved_at": {
            "type": "string"
          },
          "url": {
            "type": "string"
          }
        },
        "required": [
          "archived_text",
          "retrieved_at",
          "url"
        ]
      }
    }
  ]
}
